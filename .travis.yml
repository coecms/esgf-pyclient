language: python
python:
    - '2.7'
    - '3.4'
install:
    - pip install --upgrade pytest coverage codecov
    - pip install .
script:
    - coverage run --source pyesgf -m py.test
after_success:
    - codecov
